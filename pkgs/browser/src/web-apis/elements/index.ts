/**
 * Custom Elements exports
 *
 * Re-exports all custom elements for the Sonr browser client.
 */

export * from "./sonr-wallet.js";

/**
 * Register all custom elements
 */
export function registerAllElements() {
  // Import and register will happen automatically when modules are loaded
  // due to the auto-register at the bottom of each element file
}
