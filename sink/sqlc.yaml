version: "2"
sql:
  - engine: "sqlite"
    queries: "./migrations/query.sql"
    schema: "./migrations/schema.sql"
    gen:
      go:
        emit_interface: true
        emit_json_tags: true
        package: "models"
        out: "./models"
