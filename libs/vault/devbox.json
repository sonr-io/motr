{
  "packages": [
    "go@1.24",
    "nodejs@20",
    "bun@1"
  ],
  "shell": {
    "init_hook": [
      "export GOOS=js",
      "export GOARCH=wasm",
      "go mod download",
      "bun install"
    ],
    "scripts": {
      "build": "mkdir -p dist && GOOS=js GOARCH=wasm go build -ldflags='-s -w' -trimpath -o dist/vault.wasm . && rm -f dist/wasm_exec.js && cp wasm_exec.js dist/ && chmod 644 dist/wasm_exec.js",
     "test": "go test -v ./...",
     "lint": "go fmt ./...",
      "fmt": "go fmt ./..."
    }
  }
}
