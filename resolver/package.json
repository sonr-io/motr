{
	"name": "@sonr-io/resolver",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"build": "go run github.com/syumai/workers/cmd/workers-assets-gen -mode=go && GOOS=js GOARCH=wasm go build -o ./build/app.wasm .",
		"generate": "templ generate",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^4.10.0"
	}
}
