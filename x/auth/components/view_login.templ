package components

import (
	"github.com/sonr-io/motr/sink/options"
	"github.com/sonr-io/motr/sink/types"
	ui "github.com/sonr-io/motr/ui/base"
)

var meta = types.DefaultMetadata()

templ LoginView(opts options.LoginOptions) {
	@ui.HTML() {
		@ui.Head() {
			@ui.MetaComponent(meta)
		}
		<sl-tab-group>
			@ui.Nav() {
				@ui.NavLeft() {
					@ui.NavLogo("Motr")
				}
				@ui.NavRight() {
				}
			}
			@ui.Body() {
			}
		</sl-tab-group>
	}
}
