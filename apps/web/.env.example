# Public environment variables (exposed to client)
# These are prefixed with PUBLIC_ and available in $env/static/public

# API endpoint URL
PUBLIC_API_URL=http://localhost:5165/api

# Environment
PUBLIC_ENV=development
